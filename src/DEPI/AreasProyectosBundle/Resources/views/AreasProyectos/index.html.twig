{% extends '::backend.html.twig' %}

{% block body %}
<div class="unit-80">
    <h1>AreasProyectos list</h1>

    <div class="table-container">
        <table class="table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Areas</th>
                    <th>Proyectos</th>
                    <th>Actualizar</th>
                    <th>Consultar</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('areasproyectos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td>{{ entity.area.nombre }}</td>
                    <td>{{ entity.proyecto.nombreCorto }}</td>
                    <td>
                        <a href="{{ path('areasproyectos_edit', { 'id': entity.id }) }}">
                            <center><img src="{{ asset('bundles/backend/img/cog.png') }}" alt="" /></center>
                        </a>
                    </td>
                    <td>
                        <a href="{{ path('areasproyectos_show', { 'id': entity.id }) }}">
                            <center><img src="{{ asset('bundles/backend/img/search.png') }}" alt="" /></center>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
<a href="{{ path('areasproyectos_new') }}" class="btn btn-blue">Nuevo</a>
{% endblock %}
