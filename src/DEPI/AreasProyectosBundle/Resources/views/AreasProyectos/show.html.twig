{% extends '::backend.html.twig' %}

{% block body %}
<div class="unit-100">
    <h1>AreasProyectos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('areasproyectos') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('areasproyectos_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
</div>
{% endblock %}
