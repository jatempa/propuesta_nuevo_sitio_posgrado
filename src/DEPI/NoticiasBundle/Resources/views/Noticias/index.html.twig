{% extends '::backend.html.twig' %}

{% block mapa %}
    <li><a href="{{ path('posgradmin') }}">Inicio</a></li>
    <li><span>Noticias</span></li>
{% endblock %}

{% block body %}
<div class="unit-100">
    <div class="table-container">
        <table class="width-100 table-striped">
            <thead>
                <tr>
                    <th>Título</th>
                    <th>Contenido</th>
                    <th>Fecha de publicación</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td class="width-20 enlace">
                        <a href="{{ path('noticias_edit', { 'id': alumno.id }) }}" >
                            {{ entity.titulo }}
                        </a>
                    </td>
                    <td class="width-20 enlace">
                        <a href="{{ path('noticias_edit', { 'id': alumno.id }) }}" >
                            {{ entity.contenido }}
                        </a>
                    </td>
                    <td class="width-20 enlace">
                        <a href="{{ path('noticias_edit', { 'id': alumno.id }) }}" >
                            {% if entity.fechaPublicacion %}{{ entity.fechaPublicacion|date('Y-m-d') }}{% endif %}
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
<a href="{{ path('noticias_new') }}" class="btn btn-blue">Nuevo</a>        
{% endblock %}
