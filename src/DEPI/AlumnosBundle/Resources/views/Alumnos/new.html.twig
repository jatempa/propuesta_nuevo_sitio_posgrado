{% extends '::backend.html.twig' %}

{% block body %}
    <h3>Alumnos</h3>

    {{ form(form, {'attr': {'class': 'forms forms-inline'}}) }}
	

	<button onclick="{{ path('alumnos_new') }}">
		Ejemplo
	</button>

<ul class="record_actions">
    <li>
        <a href="{{ path('alumnos') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
