{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Posgrados creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('posgrados') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
