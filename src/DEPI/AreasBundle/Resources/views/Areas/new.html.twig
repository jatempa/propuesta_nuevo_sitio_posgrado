{% extends '::backend.html.twig' %}

{% block body %}
    <h3>Áreas</h3>

    {{ form(form, {'attr': {'class': 'forms forms-inline'}}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('areas') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
