{% extends '::backend.html.twig' %}

{% block opciones %}
    <a href="{{ path('alumnos_new') }}" class="btn">Nuevo</a>
{% endblock %}

{% block body -%}
    <h3>Áreas</h3>

<div class="table-container">
    <table class="width-60 table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th class="width-30">Nombre</th>
                <th>Descripcion</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('areas_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.descripcion }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}

{% block lateral %}
    <ul>
        <li><a href="{{ path('areasproyectos') }}">Areas-Proyectos</a></li>
    </ul>
{% endblock %}