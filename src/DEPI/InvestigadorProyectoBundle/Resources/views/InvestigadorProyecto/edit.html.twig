{% extends '::backend.html.twig' %}

{% block body -%}
    <h1>InvestigadorProyecto edit</h1>

    {{ form(edit_form, {'attr': {'class': 'forms forms-inline'}}) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('investigadorproyecto') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
