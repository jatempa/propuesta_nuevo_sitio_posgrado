{% extends '::backend.html.twig' %}

{% block mapa %}
    <li><a href="{{ path('posgradmin') }}">Inicio</a></li>
    <li><span>Investigadores</span></li>
{% endblock %}

{% block contenido %}
<div class="unit-centered unit-80">
    <div class="table-container">
        <table class="width-100 table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Apellidos</th>
                    <th>Correo electrónico</th>
                </tr>
            </thead>
            <tbody>
            {% for entity in entities %}
                <tr>
                    <td class="width-20 enlace">
                        <a href="{{ path('investigadores_edit', { 'id': entity.id }) }}" >
                            {{ entity.nombre }}
                        </a>
                    </td>
                    <td class="width-30 enlace">
                        <a href="{{ path('investigadores_edit', { 'id': entity.id }) }}" >
                            {{ entity.apellidoPaterno ~ " " ~ entity.apellidoMaterno }}
                        </a>
                    </td>
                    <td class="width-30 enlace">
                        <a href="{{ path('investigadores_edit', { 'id': entity.id }) }}" >
                            {{ entity.email }}
                        </a>
                    </td>
                    <td class="width-10"> 
                        <a href="{{ path('investigadores_edit', { 'id': entity.id }) }}" >
                            <img id="action" src="{{ asset('bundles/backend/img/editar.svg') }}" alt="Editar">
                        </a>
                    </td>
                    <td class="width-10">
                        <a href="{{ path('investigadores_delete', { 'id': entity.id }) }}" >
                            <img id="action" src="{{ asset('bundles/backend/img/eliminar.svg') }}" alt="Eliminar">
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    {{ knp_pagination_render(entities) }}
    <a href="{{ path('investigadores_new') }}" class="btn btn-blue">Nuevo</a>
</div>
{% endblock %}