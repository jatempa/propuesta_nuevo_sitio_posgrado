{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Portada list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Imagenbanner</th>
                <th>Fechapublicacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('portada_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.imagenBanner }}</td>
                <td>{% if entity.fechaPublicacion %}{{ entity.fechaPublicacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('portada_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('portada_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('portada_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
