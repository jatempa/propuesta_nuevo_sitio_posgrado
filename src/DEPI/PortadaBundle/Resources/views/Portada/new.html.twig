{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Portada creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('portada') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
